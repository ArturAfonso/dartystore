name: backend
description: um backend de estudos para um projeto de estudos DartStore.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  vaden: ^1.0.2
  postgres: ^3.3.2
  dart_jsonwebtoken: ^2.0.0
  dotenv: ^4.2.0
  mailer: ^6.0.0 # para envio de email
  uuid: ^4.3.3
  bcrypt: ^1.1.3

dev_dependencies:
  build_runner: ^2.10.3
  vaden_class_scanner: ^1.0.2
